# 变更日志 (Changelog)

本项目的所有重大变更都记录在此文件中。

本项目遵循 [语义化版本控制 (SemVer)](https://semver.org/spec/v2.0.0.html) 规范。

## [0.4.0] - 2026-09-08

### Added
- 段落与断行重组模块 (`ParagraphReconstructor`)：
  - 基于中西文标点符号、句末闭合引号实现段落自然断句与闭环。
  - 智能拼合英文行末断词连字符（De-hyphenation，如 `compre-` + `hensive` 恢复为完整单词 `comprehensive`）。
  - 内置常见英文缩写白名单（`Fig.`、`Dr.`、`e.g.`、`i.e.` 等），避免误切段落。
  - 识别段首全角空格（`\u{3000}`）、制表符与前导缩进，保留段落首行特征。
  - 重组 Word (.docx) 与 UI 检查器的数据流，将零散文本行按阅读顺序合并为完整段落，消除单行硬换行导致的异常段落间距。
- Microsoft Word (`.docx`) 生成器 (`DocxDocumentBuilder`)：
  - 遵循 Office Open XML 规范，本地生成包含段落排版与截取插图的单个文档文件。
  - 支持 Markdown 混合包 (.zip) 导出，包含排版文本与全部插图资源。
- 版面插图检测与提取模块 (`DocumentLayoutAnalyzer`)：
  - 检测 PDF 页面与扫描图像中的插图和图表区域，过滤正文行干扰并按阅读顺序穿插图文元素。
- 界面交互与视觉规范优化：
  - 遵循 Apple HIG 规范，采用原生微透材质 (`.regularMaterial`) 与超椭圆圆角样式。
  - 顶部新增三种处理模式的卡片式选择器（“直接提取文字”、“扫描件图文识别”、“强力去印识别”），支持单选与状态切换。
  - 文件导出完成后通过 `NSWorkspace` 在访达中定位对应产物。
  - 提取插图支持原生拖拽至外部应用，并提供右键另存为菜单项。
  - 水印词候选列表支持一键全选与清空，底部栏提供页数、字数及插图数量统计。
- 窗口生命周期与 Dock 唤醒支持：
  - 接入 `NSApplicationDelegate` 与 `NSWindowDelegate`，关闭窗口后保留应用实例，支持点击 Dock 图标重新显示主窗口。
- 偏好设置窗口 (`SettingsView`，快捷键 `⌘,`)：
  - 采用 Segmented 标签栏与两列网格排版，动态读取 Bundle 版本号。
- 架构限定与构建维护工具：
  - 构建与测试脚本锁定为 Apple Silicon (ARM64) 架构，新增缓存清理脚本 `clean.sh`。
- 测试套件扩充：
  - 核心逻辑、边界条件与大文件压力测试扩充至 23 项并通过验证。

### Changed
- 调整三种处理模式的描述文案为：“可选文字 + 文字水印”、“扫描正文 + 文字水印”、“扫描正文 + 纸印水印”。
- 停用 TXT 纯文本无图格式，将带插图图文排版的 Microsoft Word (`.docx`) 设为默认导出格式。
- 结果检查器重构为图文展示界面，操作控件尺寸统一对齐 macOS 标准高度（36px/38px）。

### Fixed
- 修复文本行被逐行解析为独立段落的问题，统一通过段落重组模块输出自然段落。
- 修复导入 PDF 时自动探测覆盖用户手动选择处理模式的问题，保持用户选择的优先级。
- 修复 PDF 重新载入时旧页面与插图缓存未释放的问题，在渲染与 OCR 流程中增加 `@autoreleasepool` 及时释放临时图像内存。
- 修复 Markdown Zip 压缩包覆盖写入同名文件的隐患。
- 修复 `PDFPreviewView.Coordinator` 的 deinit 显式解绑，消除通知中心潜在的无效监听。

### Removed
- 移除外部网络服务与相关模型代码（删除 `AIEndpoint.swift`、`AIProcessingEngine.swift`、`AIPromptBuilder.swift`、`OpenAIStreamParser.swift` 与 `KeychainHelper.swift`）及对应配置界面。
- 移除去水印对比分屏视口、冗余模式下拉框及非必要的印章调试术语。
- 移除对 x86_64 架构的编译配置与相关文档说明。

### Security
- 移除 `Info.plist` 中的网络权限配置与 App Transport Security (ATS) 声明，应用仅在本地执行运算。

## [0.3.0] - 2026-07-03

### Added
- 新增三类 PDF 场景模型：电子文本 + 电子水印、扫描正文 + 电子水印、全扫描件。用户选择从底层模式判断收敛为 3 个明确场景，降低配置决策成本。
- 新增 PDF 主画布与结果检查器布局。原文与 AI 结果合并到右侧检查器，主工作区从四栏缩减为三段式结构。
- 新增统一 AI 提示词构建器。默认提示词、修改留痕指令和水印残留词注入从 3 处重复实现收敛为 1 个入口。
- 新增 OCR 与导出过程的可见错误提示。Vision OCR 报错、页面渲染失败和导出失败会显示在侧边栏错误区，减少静默失败。

### Changed
- 调整扫描正文 + 电子水印的默认处理管线：默认保留原始扫描图像执行 OCR，再过滤水印残留；遮罩改为高级兜底选项，降低正文被遮罩误伤的概率。
- 优化启动页与面板视觉。移除装饰性玻璃图标底座、固定尺寸启动卡和卡片悬浮缩放，界面更接近 macOS 原生工具风格。
- 简化 AI 引擎职责。移除已废弃的后台自动落盘字段和分片写盘空实现，AI 引擎只负责按物理页输出净化文本。
- 调整状态文案。移除界面状态中的符号化提示，改为明确的“错误、完成、已取消”等文本。
- 调整构建脚本图标策略。优先复用已有有效应用图标，避免本地重建时重复触发 `iconutil` 生成失败警告。

### Removed
- 移除无效的 Toolbar 侧边栏折叠按钮。当前布局使用手写 `HSplitView`，旧按钮无法实际折叠左侧面板。
- 移除旧四栏界面组件源码：`RawTextColumn`、`AITextColumn`、`DropZoneView`、`AccessoryBarView`、`LiquidGlassContainer`。源码文件减少 5 个，避免后续维护误改旧入口。

### Fixed
- 修复扫描正文 + 电子水印场景默认开启遮罩导致 OCR 误伤正文的问题。
- 修复高级遮罩开关被场景同步逻辑反复重置的问题。用户手动设置后会保持该偏好，不再被界面刷新覆盖。
- 修复 AI 流式接口将 HTTP 错误或空流误判为空白成功结果的问题。
- 修复导出失败只写入控制台、用户界面无反馈的问题。

## [0.2.0] - 2026-06-18

### Added
- 新增 Toolbar 页码控制，支持上一页、下一页和页码输入回车跳转。

### Changed
- 将 PDF 预览调整为单页横向模式，适配触控板左右翻页。
- 将 PDF 与 AI 处理引擎标记为 `@MainActor`，减少手写主线程派发。
- 将 OCR 调用改为 `withCheckedContinuation`，移除同步等待线程阻塞。
- 在 `PDFPreviewView` 更新周期中重新绑定 Coordinator，修复页码状态同步不稳定的问题。

### Fixed
- 清理 SwiftUI 废弃修饰符，减少编译警告。

## [0.1.0] - 2026-06-17

### Added
- 新增启动导入页，支持点击选择 PDF 与拖放导入。
- 新增四栏联动工作区，支持 PDF 预览、原文结果、AI 结果按物理页同步。
- 新增 AI 净化高级选项，支持修改留痕与水印干扰词传入。
- 新增水印说明与防误伤提示。
- 新增基于 `NSSavePanel` 的原文与 AI 结果导出。

### Changed
- 精简默认 AI 系统提示词，将修改留痕拆分为可选指令。
- 取消后台自动存盘，文本结果改为内存缓存，导出由用户手动触发。
- 将去水印与正文通道配置改为卡片式选择。
- 移除 Toolbar 中的 Finder 定位按钮，降低文件位置与导出状态之间的误解。

### Fixed
- 禁用 Command+N 新建窗口，规避多窗口并发操作同一份处理状态。
- 将 PDF 实例加载移至后台，降低大文件导入时的主线程卡顿风险。
- 将结果显示改为只读文本视图，并对流式输出做增量追加，降低长文本刷新开销。
