# 变更日志 (Changelog)

本项目的所有重大变更都记录在此文件中。

本项目遵循 [语义化版本控制 (SemVer)](https://semver.org/spec/v2.0.0.html) 规范。

## [1.3.0] - 2026-08-25

### Added
- 新增基于 macOS 原生 Core Image (CIFilter) 的图像色阶预处理去水印引擎，支持在 OCR 前自动拉伸明度消除浅灰、半透明背景水印，并支持滤除红蓝彩色印章。
- 新增符合 Apple HIG 规范的首次启动欢迎开屏弹窗 (`WelcomeView`)，展示核心功能与 100% 本地隐私承诺，并支持随时从“帮助”菜单重新呼出。
- 新增待机导入空状态页 (`LaunchView`)，包含拖放视口与 3 个轻量核心能力卡片。
- 新增主工作台左右侧栏一键折叠能力（快捷键 `⌘⌥S` 与 `⌘⌥I`），释放中间 PDF 阅读空间。
- 新增结果检查器“一键复制当前页”便捷按钮，支持即时写入系统剪贴板与轻量交互反馈。

### Fixed
- 修复只读文本视图在流式输出时的滚动劫持问题，实现智能吸底算法：仅在用户处于文档底部时自动跟随滚动，用户向上翻阅时不打扰。
- 修复长篇扫描件连续 OCR 时内存累积的问题，在单页渲染、滤镜与 Vision 识别链路中引入 `@autoreleasepool` 确保即时释放位图缓冲。
- 修复 AI 多页批量处理时单页报错导致全篇任务被强行中断的问题，增加单页失败容错与结果统计。
- 修复菜单栏快捷键与界面状态未联动置灰的问题。

## [1.2.0] - 2026-07-03

### Added
- 新增三类 PDF 场景模型：电子文本 + 电子水印、扫描正文 + 电子水印、全扫描件。
- 新增 PDF 主画布与结果检查器布局，主工作区升级为三段式结构。
- 新增统一 AI 提示词构建器。
- 新增 OCR 与导出过程的可见错误提示。

### Changed
- 调整扫描正文 + 电子水印的默认处理管线，降低正文被遮罩误伤的概率。
- 优化启动页与面板视觉，移除装饰性多余组件。

### Removed
- 移除旧四栏界面组件源码：`RawTextColumn`、`AITextColumn`、`DropZoneView`、`AccessoryBarView`、`LiquidGlassContainer`。

## [1.1.0] - 2026-06-18

### Added
- 新增 Toolbar 页码控制，支持上一页、下一页和页码输入回车跳转。

### Changed
- 将 PDF 预览调整为单页横向模式，适配触控板左右翻页。
- 将 PDF 与 AI 处理引擎标记为 `@MainActor`，减少手写主线程派发。
- 将 OCR 调用改为 `withCheckedContinuation`，移除同步等待线程阻塞。

## [1.0.0] - 2026-06-17

### Added
- 初始版本发布：支持 PDF 电子文本读取、macOS 原生 Vision OCR、本地大模型连接（Ollama/LM Studio）、水印分析与 TXT/Markdown 结果导出。
