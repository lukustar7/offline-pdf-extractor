# 变更日志 (Changelog)

本项目的所有重大变更都记录在此文件中。

本项目遵循 [语义化版本控制 (SemVer)](https://semver.org/spec/v2.0.0.html) 规范。

## [1.6.0] - 2026-09-08

### Added
- 横排三大处理模式卡片选择器：在图文工作室顶部新增横排一行的 3 个等宽高质感大按钮（直接提取文字、扫描件图文识别、强力去印识别），大图标加清晰场景副标题，支持单选高亮与平滑 spring 动画切换。
- 联动去印智能预设：点击【强力去印识别】时自动确保红通道消除公章与色阶去底纹生效，简化专家参数。
- 纯血 Apple Silicon (ARM64) 架构锁定：构建脚本与测试脚本全面锁定纯血 ARM64 架构，移除 Intel x86_64 冗余。
- 一键瘦身工具 (`clean.sh`) 与 `build.sh --clean`：提供一秒清理 `.build` 编译器缓存工具，随时释放 2.0GB 磁盘空间。
- 新增三大处理模式映射一致性自动化测试，全套测试扩充至 19 项。

### Changed
- 彻底将处理模式文案白话化为“直接提取文字”、“扫描件图文识别”与“强力去印识别”，消除“电子文本 + 电子水印”等机器术语。
- 将应用版本号升级至 v1.6.0，并更新 Info.plist 与偏好设置“关于”窗口。

### Fixed
- 修复 PDF 导入时引擎强制覆盖用户偏好模式的 Bug，确保用户的自主模式选择拥有最高优先级。
- 解决 ResultInspectorView 中模式按钮在 Swift 编译器中的类型推断耗时警告，重构为独立子视图 ModeCardButton。

### Removed
- 彻底移除顶栏容易引起误解的“电子可编辑文档 (极速文本)”智能探针格式胶囊，保持顶栏原生纯净。
- 移除折叠抽屉中冗余的“处理模式”下拉菜单，所有模式选择统一收拢至顶部横排大按钮。
- 彻底移除对 Intel Mac 的兼容代码与文档说明。

## [1.5.0] - 2026-09-08

### Added
- 全新图文工作室（Document Studio）：重塑结果检查器为开阔大气的图文混排工作台，按自然阅读顺序呈现正文段落与高清内嵌插图卡片。
- 版面插图智能检测与截取引擎 (`DocumentLayoutAnalyzer`)：在 PDF 电子版和全扫描件中自动定位图表与插图区域，智能排除文字网格并无损裁剪高清插图。
- 原生 Microsoft Word (`.docx`) 单文件生成器 (`DocxDocumentBuilder`)：利用 macOS 原生 Office Open XML 引擎，将排版与插图无损内嵌为单一 Word 文件，开箱即用。
- Markdown 混合包导出：支持导出带图片附件清单的标准 Markdown 内容。

### Changed
- 废弃 TXT 纯文本无图格式，统一将支持富排版与插图的 Microsoft Word (`.docx`) 设为核心导出格式。
- 全面优化界面控件尺寸与布局，主要操作控件统一为 36px/38px 标准 macOS 大气按钮，消除拥挤微型按钮。
- 偏好设置窗口重构为极简三项（导出与插图、图像与去水印、隐私与关于）。
- 更新 `Info.plist`，彻底移除本地网络权限声明与 ATS 配置，确立 100% 纯本地离线运行模式。

### Fixed
- 彻底清理 WelcomeView 与配置描述中的残留 AI 宣传文案，消除虚假宣传隐患。
- 修复 PDF 引擎重载与重新提取时未彻底清空 `extractedPages` 导致的旧页面与插图内存滞留问题。
- 在文本层提取与 OCR 混排分析中全面接入 `autoreleasepool`，阻断大文件连续处理时的临时位图显存堆积。
- 修复导出 Markdown Zip 压缩包时覆盖写入已存在同名文件的隐患，并增加压缩退出状态校验。
- 增加 PDFPreviewView.Coordinator 的 deinit 显式解绑，确保通知中心监听零野指针泄露。
- 新增 20 页大文件合成提取与 Word 导出压力测试，全功能自动化测试扩充至 18 项全部通过。

### Removed
- 彻底剥离 AI 子系统与外部模型连接，移除 `AIEndpoint.swift`、`AIProcessingEngine.swift`、`AIPromptBuilder.swift`、`OpenAIStreamParser.swift` 与 `KeychainHelper.swift`。
- 移除应用菜单与界面中所有的 AI 净化按钮、状态标签与相关设置。

## [1.4.0] - 2026-09-03

### Added
- 智能格式探测（Auto-Probe）：导入 PDF 时自动抽样检测矢量文本层，自动匹配极速文本通道或 Vision OCR 通道，并在顶栏展示识别徽章。
- 启发式离线段落重构引擎 (`ParagraphReconstructor`)：基于中西文标点终结符判定与上下文分析，自动合并段内生硬换行，消除碎回车，并严密保护标题与序号列表。
- 科学级红通道投影印章消除与统一图像处理管道 (`DocumentImageFilter`)：利用红通道物理反射特性使红色公章融入白纸底色，配合色阶拉伸消除浅灰水印而不伤及黑色笔迹。
- 全高度沉浸式文本工作室 (Text Studio)：重构检查器视图，新增“当前页对照”与“全篇大纲”双重视图模式，支持全篇文本一览、一键复制与多格式导出。
- 高级过滤与场景参数可折叠抽屉：将水印词标签与场景覆盖收纳为抽屉面板，按需展开，释放超过 85% 的竖向阅读空间。

### Changed
- 恢复 Apple Preview.app 原生纵向连续顺滑阅读体验 (`.singlePageContinuous` + `.vertical`)。
- 全面回归 Apple HIG 原生中性光影规范，彻底移除发蓝光的呼吸阴影与过度渐变描边，使用标准语义分隔线与自然纸张投影。
- 剥离主工作画布内部冗余标题栏与右上角悬浮视口黑胶囊，大幅提升有效阅读区域。
- 精简缩略图侧栏，移除底部冗余文件卡片，使页面列表滚动更轻盈舒展。

### Fixed
- 修复待机页拖拽状态写入磁盘导致的磁盘 I/O 损耗，改用纯内存视图状态。
- 校准 `AIPromptBuilder` 修改留痕提示词与设置窗口中的 Markdown 粗体高亮描述。
- 修复在 Command Line Tools 环境下编译时因缺少 SwiftUIMacros 插件导致的构建中断问题。

### Removed
- 彻底移除工程内未被任何视图引用的冗余死代码 `Sources/FileInfoView.swift`。

## [1.3.0] - 2026-08-26

### Added
- 引入 Apple Liquid Glass（液态流光玻璃）设计系统，实现物理光折射渐变描边 (`liquidGlassBorder`) 与全透毛玻璃材质。
- 新增 36px 现代大方操作按钮高度 (`Theme.Metric.buttonHeightPrimary`)，支持 **提取文字 ⇄ 停止提取** 与 **AI 净化 ⇄ 停止净化** 动态状态切换，并在关闭窗口时自动切断后台网络流。
- 新增 Bento 场景大按钮卡片选择器，修复全扫描件官方 SF Symbol 图标 (`doc.text.image`)，实现扫描专用滤镜的上下文按需展开。
- 新增第一层级水印过滤词“标签胶囊 (Chips/Pills)”交互体系，支持疑似水印词点击快速勾选与手动输入回车即时生成胶囊。
- 新增结果检查器顶部紧凑页码与状态胶囊 (`[ Pg 1/45 ]`)，以及明确大方的 **`[ ⬇️ 导出... ]`** 下拉菜单。
- 新增物理纸张立体环境呼吸光晕 (`Theme.Shadow.paper`) 与扫描微光动效。
- 新增独立标准 macOS 偏好设置窗口 (`SettingsView`，快捷键 `⌘,`)，重构为现代 Segmented 标签栏与两列工整网格排版 (110px 标签右对齐)，彻底消除 Tab 排版挤压错位。
- 新增页面缩略图导航侧栏 (`SidebarThumbnailView`)，支持异步按需渲染页面预览、当前状态角标与快速跳页。
- 新增基于 Core Image (CIFilter) 的图像色阶预处理去水印引擎，并在主画布支持“对比去水印效果”分屏视口。
- 新增结果检查器 Markdown 富文本即时排版渲染与纯文本源码双模切换。
- 新增符合 Apple HIG 规范的首次启动欢迎弹窗 (`WelcomeView`) 与待机导入页 (`LaunchView`)。

### Changed
- 重构主工作台为“页面缩略图侧栏 + PDF 大画布 + 统一检查器”三栏架构，支持左右侧栏一键原生折叠 (`⌘⌥S` / `⌘⌥I`)。
- 彻底移除失效的小问号悬浮窗与多余的空下拉箭头，转为直观大方的副标题说明。
- 全面升级材质与色彩体系，采用纯净通透双色调色盘（深色 `#1E1E1E`/`#141416` 与浅色 `#F5F5F7`/`#ECECF0`）。

### Fixed
- 修复只读文本视图在流式输出时的滚动劫持问题，实现智能吸底算法。
- 修复多页连续 OCR 时的位图内存累积，引入 `@autoreleasepool` 即时释放缓冲。
- 修复 AI 多页批量处理时的单页容错与状态统计。

### Removed
- 移除旧式多层竖向表单侧栏 `SidebarView.swift`。
