# 变更日志 (Changelog)

本项目的所有重大变更都记录在此文件中。

本项目遵循 [语义化版本控制 (SemVer)](https://semver.org/spec/v2.0.0.html) 规范。

## [1.3.0] - 2026-08-26

### Added
- 引入 Apple Liquid Glass（液态流光玻璃）设计系统，实现物理光折射渐变描边 (`liquidGlassBorder`) 与全透毛玻璃材质。
- 新增 36px 现代大方操作按钮高度 (`Theme.Metric.buttonHeightPrimary`)，支持 **提取文字 ⇄ 停止提取** 与 **AI 净化 ⇄ 停止净化** 动态状态切换，并在关闭窗口时自动切断后台网络流。
- 新增 Bento 场景大按钮卡片选择器，修复全扫描件官方 SF Symbol 图标 (`doc.text.image`)，实现扫描专用滤镜的上下文按需展开。
- 新增第一层级水印过滤词“标签胶囊 (Chips/Pills)”交互体系，支持疑似水印词点击快速勾选与手动输入回车即时生成胶囊。
- 新增结果检查器顶部紧凑页码与状态胶囊 (`[ Pg 1/45 ]`)，以及明确大方的 **`[ ⬇️ 导出... ]`** 下拉菜单。
- 新增物理纸张立体环境呼吸光晕 (`Theme.Shadow.paper`) 与扫描微光动效。
- 新增独立标准 macOS 偏好设置窗口 (`SettingsView`，快捷键 `⌘,`)，重构为现代 Segmented 标签栏与两列工整网格排版 (110px 标签右对齐)，彻底消除 Tab 排版挤压错位。
- 新增页面缩略图导航侧栏 (`SidebarThumbnailView`)，支持异步按需渲染页面预览、当前状态角标与快速跳页。
- 新增基于 Core Image (CIFilter) 的图像色阶预处理去水印引擎，并在主画布支持“对比去水印效果”分屏视口。
- 新增结果检查器 Markdown 富文本即时排版渲染与纯文本源码双模切换。
- 新增符合 Apple HIG 规范的首次启动欢迎弹窗 (`WelcomeView`) 与待机导入页 (`LaunchView`)。

### Changed
- 重构主工作台为“页面缩略图侧栏 + PDF 大画布 + 统一检查器”三栏架构，支持左右侧栏一键原生折叠 (`⌘⌥S` / `⌘⌥I`)。
- 彻底移除失效的小问号悬浮窗与多余的空下拉箭头，转为直观大方的副标题说明。
- 全面升级材质与色彩体系，采用纯净通透双色调色盘（深色 `#1E1E1E`/`#141416` 与浅色 `#F5F5F7`/`#ECECF0`）。

### Fixed
- 修复只读文本视图在流式输出时的滚动劫持问题，实现智能吸底算法。
- 修复多页连续 OCR 时的位图内存累积，引入 `@autoreleasepool` 即时释放缓冲。
- 修复 AI 多页批量处理时的单页容错与状态统计。

### Removed
- 移除旧式多层竖向表单侧栏 `SidebarView.swift`。
